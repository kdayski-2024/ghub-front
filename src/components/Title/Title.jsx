import React from 'react';

import * as Styled from './styled';
import * as TymioUI from '../../components/index';

const Title = () => {
  return (
    <Styled.Title>
      <TymioUI.H1>Documents</TymioUI.H1>
    </Styled.Title>
  );
};

export default Title;
