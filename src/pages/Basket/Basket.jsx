import React, { useEffect } from 'react';

import * as Styled from './styled';
import * as UI from '../../components/index';
import { COLORS } from '../../models/colors';

const Basket = () => {
  return <Styled.Basket>Корзина</Styled.Basket>;
};

export default Basket;
